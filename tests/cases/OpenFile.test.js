import File from 'classes/File';

describe('File', () => {
  describe('.src', () => {
    it('loads a new file', () => {
      // const file = File.src('path');
      // expect(file.path).toEqual('path');
      // expect(file).toBeInstanceOf(File);
    });
  });

  describe('.edit', () => {});
});
